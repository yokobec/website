version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: 'Build'
    task:      
      secrets:
        - name: SSH_KEYS
      prologue:
        commands:
          - cat ~/.ssh/id_rsa_ci
          - cat ~/.ssh/id_rsa_ci.pub
          - md5sum ~/.ssh/id_rsa_ci
          - chmod 0600 ~/.ssh/id_rsa_ci
          - ssh-add ~/.ssh/id_rsa_ci
          - ssh-keyscan -H gate.sys-int.de >> ~/.ssh/known_hosts
          - checkout
      jobs:
        - name: 'Deploy Site'
          commands:
            - docker run --rm -it -v $(pwd):/src klakegg/hugo:0.80.0-ext-ubuntu-onbuild
            - ls -l
            - rsync -avz --delete -e "ssh -i ~/.ssh/id_rsa_ci -p 222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null " ./public/ website@gate.sys-int.de:/home/website/sites/yokobec.de/htdocs/
